# OpenWeb
OpenWeb is now a native desktop browser prototype for CodeOS.

Run it with:

cargo run

It opens as a real desktop application window with tabs, an address bar, and HTTPS-aware page loading.
